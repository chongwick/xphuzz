April 1:
I am working on compiling PHP with Fuzzilli's coverage.c code. I have modified coverage.c such that it is coverage.cc now. I have inserted this code into ./ext/intl/intl_convertcpp.cpp. I have set CC=clang CXX=clang++ CXXFLAGS=-fsanitize-coverage=trace-pc-guard --enable-address-sanitizer, --enable-undefined-sanitizer

April 2:
April Fools lol... yeah so anyway coverage.c needs to be included in the C compilation, not some rando cpp. let's redo this. additionally, the flags need to be for c.
1)use --enable-address-sanitizer

April 3:
./buildconf -f
insert coverage.c into main/main.c
CFLAGS="-fsanitize-coverage=trace-pc-guard"
./configure --enable-address-sanitizer --enable-undefined-sanitize

April 4:
Forcing clang does not work because php uses explicit registers

April 5:
So basically, I set export CC="clang" and export CXX="clang++" then ./configure --enable-address-sanitizer. insert coverage.c into php_cli.c. edit the makefile and replace fsanitize=address with -fsanitize-coverage=trace-pc-guard. then make.

April 8:
Got php working: singularity run ubuntu.sif ./php <input_file>

April 11:
Rewrite libJSEngine in Python OR get it working... idk. Also, fsanitize=address should be active... potentially use two engines, one with fsanitize=address and one with trace-pc-guard

CAN YOU FUZZ A HASHTABLE?

can't do both fsanitize=address and trace-pc-guard bc seg fault... idk why

April 19:
We need a second engine (i.e. fsanitize=address). Additionally, there are difficulties with running an interactive shell and keeping the communication pipe open using python https://www.reddit.com/r/learnpython/comments/qplpyn/how_to_run_a_command_line_interactive_program/. This will be addressed later, but for now, we'll just close the pipe. https://docs.python.org/2/library/pty.html
Make python script wait a little bit before reading from the shared bitmap focus on coverage consistency

April 22:
JavaScript-Raider also has a variance of a few hundred edges, so we're okay.

we're using a100 bla bla bla...

April 24:
We got shared memory saving and loading
We have two php engines. one with coverage instrumentation compiled using clang. the other with undefined/address sanitizers enabled compiled using gcc.

April 25:
need to fix stderr/stdout
work on translation portion.

April 26:
stderr/out done. needs to get parsed. Fixed star_llama and downloaded new model. I think llama 3 is better than codellama...

April 27:
We have star_llama and receiver configured for llama3. llama3 follows instructions significantly better than I thought it would. 

May 9:
Every Query by nature clears the context via change_role
We need some kind of time out for the executions
Maybe do some sort of queuing system for the llm 

May 10:
All file handling through the llm_workdir should be handled by phuzz.py to prevent editing the same file at the same time
__pycache__
config

May 11:
success seems to be 80%
We need a queuing system. -> we queue up requests to the LLM i.e. fixes and translations

May 13:
So we can't use a shared memory region for the queues because then we can't share the queues across multiple systems. We don't need to check sanitizer output during the initial mapping as the coverage engine does not have sanitization capabilities
