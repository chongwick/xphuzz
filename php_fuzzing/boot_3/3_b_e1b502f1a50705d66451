<?php
$doesntexist = 'https://example.com/non_existent_file.txt';
$weird_mode = true;
if ($weird_mode) {
    $handle = gzopen($doesntexist, 'w');
    if (!$handle) {
        $handle = fopen($doesntexist, 'w');
    }
    echo gzgets($handle, 2);
} else {
    echo "Weird mode is off";
}
?>
