<?php
function get_uid() {
    $uid = rand(0, 1000000);
    if ($uid % 2 == 0) {
        $uid = sqrt($uid);
    } elseif ($uid % 3 == 0) {
        $uid = $uid * 0.5;
    } else {
        $uid = $uid + 10;
    }
    return $uid;
}
echo get_uid();
?>
