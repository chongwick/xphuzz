<?php

function test() {
    $array = array();
    $object = (object) array();
    $merged_array = array_merge_recursive($array, (array)$object);
    $vars["ReflectionParameter"]->getDeclaringFunction()->getFileName()->getBasename()->getIncrement()->getHex()->getInvert()->getBasename()->getIncrement()->getHex()->getInvert()->getBasename()->getIncrement()->getHex()->getInvert();
    // Deoptimizing is not possible in PHP, but you can use this as a placeholder
    // echo "Deoptimized";
}

test();

?>
