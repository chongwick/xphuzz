<?php
function get_defined_constants() {
    $result = array();
    $namespace = 'constant_';
    foreach (get_defined_functions() as $name => $value) {
        if (substr($name, 0, strlen($namespace)) == $namespace) {
            $result[$name] = $value;
        }
    }
    return $result;
}

?>