<?php
$vars["SimpleXMLElement"]->addAttribute(str_repeat(chr(13), 257), 
"this is a really long string that will probably cause issues with your XML document, especially when combined with the subsequent values which are also very large, because I used the chr() function to create a bunch of special characters and also used bin2hex() to convert some of the strings to hexadecimal, which will definitely cause some sort of error or warning because it's just not what you're supposed to do in PHP, and also I used some large numbers in the subsequent values, and maybe even a float number because I'm feeling fancy, oh and did I mention that the strings I used are really, really long and will probably cause the XML document to exceed the maximum allowed size, which is usually around 10MB or so, or maybe even 50MB or 100MB depending on the specific application or software you're using, and I'm not even going to get into the potential security issues of using unescaped user input in an XML attribute, because that would be way too obvious..."); 
$vars["SimpleXMLElement"]->addAttribute(str_repeat(chr(193), 257). str_repeat(chr(155), 17). str_repeat(chr(147), 4097), 
"another super long string that will also definitely cause issues with your XML document, especially when combined with the previous value, because we're talking about a string that's going to be at least a few hundred thousand characters long, and maybe even a million or more, depending on how you count it, and we're not even getting into the potential encoding issues with special characters, because that would be way too complicated, and I'm not even going to get into the potential performance issues with parsing such a large string, because that would be way too obvious..."); 
$vars["SimpleXMLElement"]->addAttribute(str_repeat(chr(161), 65537). str_repeat(chr(213), 1025). str_repeat(chr(214), 1025), 
"a float number that's supposed to be really, really small, but I don't think it's going to make a difference in the grand scheme of things, because we're already talking about a few million characters in the previous strings, so a few more thousand shouldn't really make a difference, but I guess it's always nice to include a float number in a XML attribute just to see what happens, especially when combined with the other values, because who knows what kind of magic will happen when the XML parser tries to parse this document... 2.23431234213480e-400");
?>
