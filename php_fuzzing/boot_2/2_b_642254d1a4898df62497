<?php
strnatcmp(
    implode(array_map(function($c) {
        return chr(255 - $c). chr(255 - $c). chr(255 - $c);
    }, range(0, 255))),
    implode(array_map(function($c) {
        return "\\x". str_pad(dechex($c ^ 0x13), 2, "0");
    }, range(0, 255)))
);

?>