<?php
$vars["SimpleXMLElement"]->addAttribute(
    // Use a random number
    str_repeat(chr(mt_rand()), 257),
    // Concatenate strings, integers, and floats
    bin2hex(str_repeat(chr(193), 257). str_repeat(chr(155), 17). chr(147). 4097. str_repeat(chr(161), 65537). chr(213). str_repeat(chr(214), 1025). 2.23431234213480e-400),
    // Use a random hex color
    bin2hex(hex2rgb("FF". str_shuffle("0123456789ABCDEF"). "00")));
?>
