<?php
$doesntexist = "/tmp/doesntexist";
$weirdmode = true;
if ($weirdmode) {
    $zip = new ZipArchive();
    $zip->open($doesntexist, ZIPARCHIVE::CREATE);
    $zip->addFromString("non-existent-file.txt", str_repeat("A", 0x100));
    $zip->close();
} else {
    zip_entry_open(fopen($doesntexist, "w"), fopen($doesntexist, "w"), str_repeat("A", 0x100));
}
?>
